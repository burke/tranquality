#!/usr/local/bin/ruby -w

require 'tranquality'
require 'tranquality/option_parser'

require 'flog'

options = Tranquality::OptionParser.parse(ARGV)

ARGV << "-" if ARGV.empty?

runner = Tranquality::Runner.new(options)
runner.run(ARGV)
runner.report

exit 0
